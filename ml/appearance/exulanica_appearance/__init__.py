"""Generated appearance on exact structure, outside the product.

Structure is the truth and a model supplies appearance only. This package captures a world's exact
structure (depth, normals, per-pixel identity, edges), reads and writes the records that make a
model's output a versioned data object, and measures outputs against the structure and against the
procedural look. It imports nothing from ``exulanica`` and reads files only.
"""
