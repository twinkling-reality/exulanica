`compressor-3.3.3-format-only.sog` is a 16,168-byte output from the pinned
`@playcanvas/splat-transform` 3.3.3 compressor (512 generated test Gaussians).
SHA-256: `cc165ad93f290bbce4492f54d469fdb9daf436cab8b671ec8ae15e87961b7ade`.

It tests format interoperability only: SOG metadata version 2, ZIP STORE, UTF-8,
and streamed signed data descriptors. It is not a photographic reconstruction,
reference scene, quality evaluation, or browser screenshot. It is never loaded by
the production application. The tests supply it through the same authenticated
geometry boundary as production and separately test rejection before native decoding.
