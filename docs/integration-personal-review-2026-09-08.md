# Personal admission and manual review integration, 2026-09-08

Follow-up: the shared admission-policy gap described here was subsequently addressed in
`integration-screening-currency-2026-09-08.md`. Asset-read currency remains open. This report
retains the state and limitations of its own verified integration.

Both feature branches are integrated. This report distinguishes executed generated-fixture paths
from retained personal-data operation. No public migration or paid compute was performed.

| Candidate | Independently tested head | Backend | Web | Other gates |
| --- | --- | --- | --- | --- |
| Manual review, reserved wiring and pending-mask identity fix | `2aa55a5` | 2059 passed, 3 skipped | 876 passed | Ruff, imports, typecheck, boundaries passed |
| Admission rebased onto that integration | `266b8cb` | 2072 passed, 3 skipped | 876 passed | Ruff, imports, typecheck, boundaries passed |

The integration verification record is `evaluation/2026-09-08-personal-review-integration.json`.
It retains all fourteen environment/gate logs and follows the final admission evidence record.
The verified executable tree is `266b8cbd2d4d68bd0556eee606d601f7a3a2cdd6`; subsequent changes
in this integration are documentation and generated verification evidence only.

## What works in executed fixtures

Manual review now supports adding a missed person through the real authenticated route. The
browser check drew on an 800 by 400 generated photograph, saved the exact normalized outline and
owner, fully reloaded the app, and reopened the saved region. The second photograph stayed
unscreened. No subject or consent was invented. When the required mask is absent, the image and
save controls stay unavailable while the review identity remains accessible to its workspace.
Removing the editor mount failed the named browser path; restoring the exact source passed it.
Both scratch schemas and fixture services were cleaned up. The orchestrator inspected the
retained screenshot, stored-row evidence, source digests and all record bindings.

The original frontend file set missed one production handoff: unavailable source metadata
discarded capture identity. The approved extension was limited to `_source_from_row` in
`exulanica/world/repository.py` and two existing test files. Tombstone, deletion and workspace
refusals remain in place. The original citation resolver's policy was not changed by this fix.
`main.ts` was edited only through the orchestrator's serialized integration patch. An identified
detached-panel callback retention issue was corrected before acceptance.

Personal admission provides a runnable exact-byte authority, manual review, real mask,
re-screening and retry workflow using existing receipt primitives. Executed controls include
wrong bytes/workspace, missing/expired authority, detection-only geometry refusal, stale-mask
refusal at the new command, exact edit replay and conflicting replay. Existing frontier preflight
accepted two persisted source screenings and the schema; its overall run deliberately stopped at
the absent signing key. This was not a completed personal frontier demonstration.

## Findings and limitations retained through integration

The shared SQL predicate can still accept a historical eligible screening after changed privacy
inputs. The new Python guard does not close direct frontier or scene-admission SQL consumers.
The evidence explicitly separates this known failing baseline from the killed command-guard
mutant. No stale depth write or disclosure was demonstrated. Shared policy closure remains a
prerequisite for activating the flow on real personal media.

The admission owner's first full suite reported 2070 passed, 3 skipped and one retained-evidence
failure caused by personal absolute paths in command strings. Two unaccepted candidate records
were regenerated through a corrected recorder under a narrow pre-integration authorization;
defective copies were not moved aside to bypass the check. The branch history was corrected,
its replacement provenance retained, and the final independent full suite above passed.
This exception did not rewrite accepted records on main.

The orchestrator's proposed schema-fallback defect was itself wrong: `applied_migrations` already
qualifies migration history with current_schema. The callee disproved the finding. An executed
command regression now confirms no fallback writes; no duplicate guard was added.

Manual region keys are distinct random identifiers. They preserve separate manual drafts but
do not reconcile future detector proposals automatically. Offline receipt verification cannot
discover later withdrawals. Account-owner attestations are not subject authentication.

## Deployment and next work

After both suites, retained public was read at migration 0038, with
284 captures, 880 artifacts, 5
scenes and 0 person regions. Code includes migration 0039; applying it
to retained public remains an explicit deployment action. No migration was added by these tasks.

The next proposed serialized brief is `briefs/2026-09-08-stale-screening-policy.md`, with migration
0040 reserved for dispatch after this integration. It takes priority over the queued World Read
evidence brief before personal-data activation. No implementation task for it has been started.
The audit and original three copy-ready briefs are retained beside that follow-up. Main has not
been pushed; remote backup and a tested database/artifact recovery plan remain operator work.
