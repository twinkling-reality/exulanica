# Posed views and recovery assessment integration

The operator authorized local integration of both completed branches. Posed-view owner tip
67f5811 and recovery assessment ad69384 merged without conflicts; combined merge head is
4324a351353ce6faf786cfcb0073cc6497717c7e. No push or retained-data activation occurred.

## Verification and resource use

All non-documentation files at the combined head are byte-identical to the owner's tested
source 56a13a2. Full-gate results are reused rather than rerun: 2180 backend passes, 3 skips,
876 web passes, Ruff, four import contracts, typecheck and boundaries. The backend run took
397.43 seconds. This is owner-executed full-suite evidence, not a second independent suite.

Independent integration checks reproduced both canonical envelope hashes and predecessor
bindings, verified 295 tracked artifact entries across the two records, and checked all three
exact selector FAILED lines. Fresh processes outside the repository, with database environment
settings removed, replayed the saved scene and place downloads using the integrated code.
Each matched its saved verification result for three images. No database was required.

The generated integration record is evaluation/2026-09-08-posed-view-integration.json. It binds
the tested/integrated heads, predecessor, control selectors and replay digests. Focused retained
record checks passed all 21 cases after final envelope generation. The first unpublished envelope
used the wrong outer profile label; the canonical-record test rejected it. The serializer output
was corrected to exulanica.digest-bound-record/v1 and the focused checks reran successfully.
The record body and digest were unchanged. Accepted historical evidence is unchanged.

## What is now available

World Read can advertise authenticated, exact-byte downloads tied to retained pose inputs and
camera calibration. Existing current-permission checks remain in force. Replacement masks,
changed consent or lineage require refresh or return refusal rather than serving different
bytes under an old binding. Identity pixel transforms only are supported: non-identity EXIF
originals remain unavailable. The successful oriented-source case delivers a real upright mask
whose bytes match the scripted pose input. No real reconstruction-quality claim follows.

The recovery assessment and two proposed successor briefs are integrated. Existing dump/restore
and deletion-replay tests do not establish deployment recovery. Source checkpoint sealing,
worker shutdown/marker wiring, independently retained deletion authority and later consent
history need explicit handling. No isolated drill or retained upgrade has been executed here.

## Next execution boundary

Prepare the generated-media isolated recovery drill from its integrated brief, using targeted
checks rather than another full backend campaign. Its exact execution manifest and database slot
must be settled before running it. The drill brief's earlier posed-view reservation is historical;
that slot is now free. Do not treat the many approval phases in the retained-activation draft as
automatic dispatch or as a requirement to seek the same authorization repeatedly. A concrete
operator-approved scope can cover related steps while preserving the distinct retained-data
boundary. Private backup destination and custody remain operator decisions.

A hands-on walkthrough on generated data can expose UI and workflow gaps independently of
retained activation. Review the separate README task when it reports ready. No additional backend
feature package, cloud service, model run or migration is dispatched by this integration.
